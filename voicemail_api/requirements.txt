# Runtime dependencies for server.py (voicemail API).
# server.py only imports fastapi + httpx and runs under uvicorn; everything else
# it uses (audioop, wave, io, hashlib, hmac, subprocess, ...) is the Python
# stdlib. python-multipart is required by FastAPI for file/form uploads.
# Pinned to the versions running on the source server.
fastapi==0.136.1
uvicorn[standard]==0.46.0
httpx==0.28.1
python-multipart==0.0.28

# client.py example helper (optional at runtime).
requests==2.34.2

# audioop was removed from the Python stdlib in 3.13; restore it there.
audioop-lts==0.2.2 ; python_version >= "3.13"
